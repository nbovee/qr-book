[project]
name = "qr-book"
version = "0.1.0"
description = "Generate PDF pages with QR codes for item tracking"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "qrcode[pil]>=7.4.0",
    "reportlab>=4.0.0",
]
